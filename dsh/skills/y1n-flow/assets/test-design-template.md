# {模块名称} - 测试设计文档

> **创建时间**：{日期}
> **关联计划**：{计划文档路径}
> **状态**：草稿 | 已评审 | 执行中 | 已完成

## 模块概述

{简要描述本模块的职责和边界}

## 测试场景

### 场景 1：{场景名称}

**Feature**：{功能描述}

```gherkin
Scenario: {场景名称}
  Given {前置条件}
  When {操作}
  Then {预期结果}
```

**边界情况**：

```gherkin
Scenario: {边界场景名称}
  Given {边界前置条件}
  When {边界操作}
  Then {预期结果}
```

---

### 场景 2：{场景名称}

**Feature**：{功能描述}

```gherkin
Scenario: {场景名称}
  Given {前置条件}
  When {操作}
  Then {预期结果}
```

---

## 不测什么（显式排除）

- {说明哪些不在本模块测试范围内，避免测试越界}

## 依赖与 Mock

| 依赖项 | Mock 方式 | 说明 |
|--------|-----------|------|
| `{依赖名}` | {stub/mock/fake} | {为什么这样 mock} |
